# Quiet Window AGENTS Guide

## 문서 목적
이 문서는 Quiet Window 프로젝트에서 AI 도우미와 개발자가 같은 기준으로 작업하기 위한 상시 참조 가이드다.

## 1 프로젝트가 해결하려는 것
- 사용자가 풍경을 보면서 자신에게 맞는 백색소음 조합을 만들고, 휴식과 집중을 돕는 경험을 제공한다.
- 음악 피로(귀 피로, 반복 청취 피로)를 줄이고, 부담 없는 배경 사운드 환경을 제공한다.
- 모바일에서 쉽게 접속 가능한 웹앱 형태로 빠르게 사용 가능하게 한다.

구성 이유: 프로젝트의 목적이 흔들리면 기능 우선순위가 무너지므로, 먼저 문제 정의를 고정한다.

## 2 최종 기술 스택과 고른 이유
- 앱 형태: 모바일 중심 웹앱
  - 이유: 앱스토어/플레이스토어 배포 비용 없이 바로 배포하고 사용 가능.
- 지원 브라우저: Chrome 중심
  - 이유: 초기 범위를 좁혀 품질과 구현 난이도를 관리.
- 프론트엔드: 웹 표준 기반 구현 (HTML/CSS/JavaScript)
  - 이유: 초보자 기준으로 구조가 단순하고 디버깅이 쉬움.
- 풍경 렌더링: 로컬 2D 이미지 파일 표시
  - 이유: 3D 대비 구현 복잡도와 리소스 부담이 낮음.
- 오디오: mp3 파일 기반 재생/믹싱
  - 이유: 준비한 소스를 그대로 사용 가능하고 호환성이 좋음.
  - 파일명 규칙: 소문자 + 하이픈(kebab-case) 사용 (예: `sound-cafe-chatter.mp3`)
- 오디오 제어: 브라우저 오디오 제어 방식 사용 (볼륨 슬라이더, 재생/정지 토글)
  - 이유: 풍경별 기본 볼륨(50%/0%) 정책과 다중 믹싱 요구를 직접 구현 가능.
- 배포: Azure(클라우드) 기반 웹 배포
  - 이유: 사용자가 목표로 한 배포 방향이며, 웹앱 운영에 적합.

구성 이유: 기술 선택의 이유를 함께 적어두면, 나중에 변경이 필요할 때 판단 근거를 잃지 않는다.

## 3 만들 핵심 기능
- 화면 구조(확정안)
  - 시작 화면: 중앙 로고 + 하단 `시작하기`/`설정`/`나가기` 버튼
  - 풍경 선택: 단일 카드(이미지/이름/설명) + 좌우 화살표 이동 + 현재 위치(예: 2/5) + 하단 선택 버튼
  - 메인 화면: 상단 풍경명/날씨/시간대, 중앙 풍경 배경, 하단 사운드 믹서 버튼
  - 믹서 패널: 버튼으로 열고 닫는 구조, 4개 기본 노출 + 가로 스크롤, 아이콘/텍스트/슬라이더/값 표기
- 고정된 5개 풍경 선택/전환
  1. 비 오는 밤에 다락방의 창문
  2. 카페 창가
  3. 우주정거장의 창밖
  4. 숲속 오두막의 창문
  5. 열차 침대칸의 창문
- 풍경 운영 정책
  - 초기 5개 풍경은 고정이며 삭제하지 않음
  - 배포 이후 운영자(작성자)가 필요 시 풍경을 추가할 수 있음
  - 사용자가 풍경을 직접 추가/삭제하는 기능은 제공하지 않음
- 풍경별 기본 소리 자동 시작
  - 해당 풍경 기본 소리 50%
  - 나머지 소리 0%
  - 기본 매핑: 다락방의 창문=빗소리, 카페 창가=사람들이 웅성거리는 소리, 우주정거장의 창밖=화이트 노이즈, 숲속 오두막의 창문=장작 타는 소리, 열차 침대칸의 창문=기차소리
- 다중 사운드 믹싱
  - 8개 사운드 각각 슬라이더(0~100) 조절
  - 슬라이더 값 숫자 표시
- 특수 재생 규칙
  - 연필 소리: 반복 재생
  - 책 넘기는 소리: 40~60초 랜덤 재생
- 풍경 옵션 수동 변경
  - 날씨 변경
  - 시간대 변경
- 접근성 최소 기준
  - 스페이스바로 재생/정지(볼륨 0% <-> 이전 볼륨 복원)
  - 사운드 컨트롤에 아이콘 + 텍스트 라벨 제공
  - 첫 사용자 상호작용 이후 오디오 재생(브라우저 자동재생 정책 준수)

구성 이유: 구현 범위를 기능 단위로 고정하면, 개발 중 범위 확장(스코프 크리프)을 막을 수 있다.

## 4 이번엔 만들지 않을 것
- 3D 풍경 구현
- 풍경 전환 시 자동 애니메이션/자동 순환 전환
- 사용자가 풍경을 직접 추가/삭제하는 기능
- 기본 5개 풍경 삭제 기능
- 네이티브 앱(앱스토어/플레이스토어) 배포
- 창문 낙서 기능(후순위, 가장 나중)

구성 이유: 이번 릴리스에서 안 만드는 것을 명확히 해야 일정 지연을 줄일 수 있다.

## 5 협업 규칙 (초보자 친화)
- 사용자는 코딩 초보다. 설명은 짧고 쉬운 문장으로 작성한다.
- 전문 용어가 나오면 바로 괄호로 짧게 뜻을 붙인다.
  - 예: 배포(웹앱을 인터넷에 올려 다른 사람이 접속 가능하게 만드는 작업)
- 한 번에 큰 변경보다, 단계별로 작게 구현하고 바로 확인한다.
- 파일 수정 시 왜 바꾸는지 먼저 설명하고, 변경 후 결과를 명확히 요약한다.
- 요구사항에 없는 내용을 임의로 추가하지 않는다.
- 아이콘만으로 의미를 전달하지 말고 텍스트를 함께 표시한다.
- 숨김 UI(예: 믹서 패널)는 버튼 상태(열림/닫힘)가 눈에 보이게 만든다.

구성 이유: 소통 규칙을 고정하면 학습 속도와 개발 안정성이 함께 올라간다.

## 운영 메모
- 이 문서는 PRD와 함께 프로젝트의 기준 문서로 유지한다.
- 요구사항이 바뀌면 PRD와 이 문서를 같은 커밋에서 함께 업데이트한다.