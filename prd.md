# PRD: Quiet Window (나만의 백색소음 & 풍경 스튜디오)

## 1. 문서 개요
- 제품명: Quiet Window
- 작성일: 8월 1일
- 작성자: 김태후

## 2. 제품 컨셉
3D/2D로 구현된 창문 너머 풍경(예: 비 오는 밤, 노을 지는 언덕, 눈 내리는 숲 등)을 감상하면서, 사용자가 백색소음을 조합해 듣는 앱.

## 3. 목표
- 풍경 감상 경험 제공
- 볼륨 조절 기반 사운드 믹서 제공

## 4. 타깃 사용자
- 페르소나 A
1. 이름: 김지구 (가칭)
2. 연령/상황: 중학교 3학년, 하고 싶은 일이 많아 피로가 누적된 상태
3. 사용 맥락: 휴식이 필요하지만, 주변 소리가 없으면 집중이 잘 안 됨
4. 현재 대안/불편: 음악을 들어왔으나 귀가 아프고 음악 자체에도 피로를 느낌
5. 니즈: 부담 없는 배경음과 풍경으로 편안함과 집중감을 동시에 얻고 싶음

## 5. 범위
### 5.1 포함 범위 (In Scope)
- 풍경 선택/감상
- 다중 사운드 믹싱 (슬라이더 0~100)
- 풍경별 기본 사운드 매핑
- 풍경 요소 변경: 날씨, 시간대
- 웹 우선 배포
- 초보자 기준으로 Azure(클라우드) 관련 방식의 웹 배포를 목표로 함
- 모바일은 스토어 배포 대신 웹 바로가기(홈 화면 추가) 방식으로 사용

### 5.2 제외 또는 후순위 (Out of Scope / Later)
- 창문 낙서 기능: 가장 나중에 구현 예정

## 6. 핵심 경험 시나리오
사용자는 원하는 풍경을 선택하고, 각 사운드 볼륨을 조절해 자신만의 백색소음을 만들어 감상한다.

## 7. 기능 요구사항
### 7.0 화면 구성 (확정안)
- 시작 화면
1. 중앙에 `Quiet Window` 로고를 표시
2. 하단에 `시작하기`, `설정`, `나가기` 버튼을 표시
3. 배경은 Deep Navy 단일 색 기반에 별/야경 느낌의 가벼운 장식 요소를 허용

- 풍경 선택 화면
1. 한 번에 1개의 풍경 카드(이미지 + 풍경 이름 + 한 줄 분위기 설명) 표시
2. 좌/우 화살표로 다음/이전 풍경 카드 이동
3. 현재 위치 표시(예: 2/5) 제공
4. 하단에 `선택` 버튼 제공

- 메인 화면
1. 상단: 현재 풍경 이름 + 날씨 버튼 + 시간대 버튼
2. 중앙: 현재 풍경 이미지를 배경 중심으로 표시
3. 하단: `사운드 믹서` 버튼을 상시 표시

- 믹서 패널
1. `사운드 믹서` 버튼을 눌렀을 때 패널이 열리고, 다시 누르면 닫힘
2. 기본 4개 항목을 먼저 보여주고 가로 스크롤로 나머지 항목 접근
3. 각 항목은 `아이콘 -> 텍스트 -> 슬라이더 -> 값(0~100)` 순서로 표시
4. 믹서 버튼에 열림/닫힘 상태를 텍스트로 함께 표시

### 7.1 풍경
- 기본 풍경 목록(초기안):
  1. 다락방의 창문
  2. 카페 창가
  3. 우주정거장의 창밖
  4. 숲속 오두막의 창문
  5. 열차 침대칸의 창문

- 풍경은 아래 내용이 변경 가능해야 함.
1. 날씨: 비, 눈, 안개 등
2. 시간대: 낮, 노을, 밤 등

- 풍경 개수 정책:
1. 초기 5개 풍경 고정
2. 초기 5개 풍경은 삭제하지 않음
3. 사용자는 풍경을 직접 추가/삭제하지 않음
4. 배포 이후 운영자(작성자)가 필요 시 풍경을 추가할 수 있음
5. 풍경 전환 기본 범위는 초기 5개 풍경

- 풍경 렌더링 방식:
1. 로컬 이미지 파일을 웹에 표시하는 2D 이미지 중심 방식
2. 3D 구현은 현재 범위에 포함하지 않음

- 날씨/시간대 변경 방식:
1. 수동 변경
2. 변경 시 애니메이션 전환은 현재 범위에서 제외

### 7.2 사운드 믹서
- 사운드 목록:
1. 빗소리
2. 바람소리
3. 천둥소리
4. 장작 타는 소리
5. 사람들이 웅성거리는 소리
6. 연필 소리(자주) / 책 넘기는 소리(가끔)
7. 기차소리
8. 화이트 노이즈

- 각 사운드는 슬라이더(0~100)로 볼륨 조절 가능해야 함.
- 사운드 믹서 UI는 기본적으로 버튼으로 열어 사용하는 패널형 구조를 사용함.
- 각 사운드 컨트롤은 아이콘만 단독으로 쓰지 않고 텍스트 라벨과 현재 값을 함께 표시해야 함.

### 7.3 풍경 전환 시 기본 오디오 상태
- 풍경 전환 시 해당 풍경의 기본 사운드만 자동으로 약 50% 볼륨으로 시작.
- 나머지 사운드는 0%로 시작.
- 기본 매핑 표:

| 풍경 이름 | 기본 사운드(자동 50%) |
| --- | --- |
| 다락방의 창문 | 빗소리 |
| 카페 창가 | 사람들이 웅성거리는 소리 |
| 우주정거장의 창밖 | 화이트 노이즈 |
| 숲속 오두막의 창문 | 장작 타는 소리 |
| 열차 침대칸의 창문 | 기차소리 |

### 7.4 특수 재생 규칙
- 연필 소리: 반복 재생(loop)
- 책 넘기는 소리: 40~60초 사이 랜덤 간격으로 1회씩 재생

### 7.5 오디오 포맷 정책
- 제공/적용 포맷은 mp3로 한정
- mp3 파일명 규칙: 소문자 + 하이픈(kebab-case) 사용 (예: `sound-white-noise.mp3`)

### 7.6 후순위 기능
- 창문 낙서 기능:
1. 이미지 위에 투명한 `<canvas>` 레이어 추가
2. 마우스 드래그로 유리창 낙서 효과 구현

## 8. 콘텐츠/자산 제작 계획
### 8.1 풍경 이미지
- 제작/수급 후보:
1. Midjourney
2. Leonardo.ai
- 추가 효과: 이미지 위 비 내리는 효과

### 8.2 사운드 소스
- 수급 후보:
1. Freesound.org (상업적 웹앱 사용 가능 음원 검색)
2. Pixabay Audio
3. BBC Sound Effects
- 대안: Web Audio API 기반 실시간 합성

## 9. 파일/구현 메모
- 이미지:
1. `images` 폴더 생성
2. 예시 파일: `attic_rain.jpg`, `space_station.jpg` 등

- 사운드:
1. `sounds` 폴더 생성
2. mp3 파일 연동

## 10. 비기능 요구사항
- 성능: [확인 필요: 목표 로딩 시간/프레임 기준]
- 호환성:
1. 지원 브라우저 범위: Chrome 중심으로 제한
2. 배포 형태: 모바일에서 동작하는 웹앱 중심
3. 최소 사양: 모바일 브라우저 사용 가능 환경
- 접근성(최소 적용 기준, MVP)
1. 입력 방식 정책
- 풍경 선택과 슬라이더 조절은 마우스/터치 중심으로 조작
- 키보드 조작은 재생/정지 토글에 한정
- 스페이스바로 재생/정지(볼륨 0% <-> 이전 볼륨 상태 복원) 토글 제공
2. 슬라이더/버튼 의미 전달
- 모든 사운드 컨트롤에 시각 요소(아이콘) + 텍스트 라벨(예: Rainy) 함께 제공
- 모든 버튼과 슬라이더에는 이름(레이블)을 제공
- 사운드 슬라이더는 현재 값(0~100)을 컨트롤 하단 텍스트로 항상 표시
3. 가독성
- 본문 텍스트 대비는 WCAG AA 기준(일반 텍스트 4.5:1 이상) 준수
- 텍스트 크기는 모바일 기본 가독성을 해치지 않도록 유지(브라우저 확대 시 레이아웃이 깨지지 않음)
4. 터치 조작
- 모바일 터치 타깃(버튼/핸들)은 최소 44x44px 권장
- 오조작 방지를 위해 인접 컨트롤 간 간격 확보
5. 오디오 재생 안전장치
- 브라우저 자동 재생 정책(Autoplay Policy)에 따라 첫 사용자 상호작용(예: 화면 클릭) 이후 오디오 재생
- 오디오가 재생 중/정지 상태인지 화면에 명확히 표시
6. 모션/효과
- 과도한 자동 애니메이션은 사용하지 않음
- 향후 모션 도입 시 끄기 옵션 또는 `prefers-reduced-motion` 대응

- 접근성 수동 점검 체크리스트(출시 전)
1. 스페이스바로 재생/정지(볼륨 0% <-> 이전 볼륨 복원) 토글이 동작함
2. 사운드 컨트롤에 아이콘+텍스트 라벨이 함께 표시됨
3. 슬라이더 이름과 현재 값(숫자)이 화면에 표시됨
4. 텍스트 대비가 낮아 읽기 어려운 구간 없음
5. 모바일에서 버튼이 너무 작지 않음
6. 첫 클릭 전 자동 재생이 발생하지 않음
- 저작권/라이선스: 상업적 이용 가능한 풍경/사운드 소스만 사용

## 11. 성공 지표
- 별도 정량 KPI를 두지 않음

## 12. 일정 및 우선순위
- 출시 목표일: 8월 15일

- 우선 구현:
1. 풍경 감상
2. 사운드 믹서(0~100 슬라이더)
3. 풍경별 기본 사운드 자동 설정
4. 연필/책 넘김 특수 재생 규칙
5. 웹 배포

- 후순위 구현:
1. 창문 낙서 기능

- 우선순위 확정표 (기능별 Must/Should/Could)
1. Must
- 풍경 선택
- 다중 사운드 믹싱(슬라이더)
- 풍경별 기본 소리 50%, 나머지 0% 시작
- 풍경 전환은 정의된 5개 풍경 내에서 수행
- 믹싱 가능한 8개 사운드 제공
- 풍경 내 날씨/시간대 변경
- 웹 배포 우선
2. Could/Later
- 창문 낙서 기능(가장 나중에 구현)
